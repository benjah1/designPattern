# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'livereload' do
  watch(%r{app/[^/]+(/[^/]+)?$})
end

guard 'sass', :input => 'app/sass', :output => 'app/css', :load_paths => ['~/.rvm/gems/ruby-1.9.3-p392/gems/zurb-foundation-4.2.3/scss']
